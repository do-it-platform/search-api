query {
    jobs(location: { latitude: 51.0, longitude: -2.0 }) {
        id,
        vendorId,
        title,
        description,
        latitude,
        longitude,
        payment,
        createdAt,
        modifiedAt
    }
}